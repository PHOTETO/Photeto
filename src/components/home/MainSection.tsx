import styled from "styled-components";

const MainSection = () => {
    return (
        <Container>

        </Container>
    );
};

const Container = styled.div`
    position: relative;
`;

export default MainSection;
